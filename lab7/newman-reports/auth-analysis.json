{
  "problem": "All API requests are returning 401 Unauthorized",
  "causes": [
    "API requires authentication but no auth headers are configured",
    "Missing Authorization header in requests",
    "No login/authentication endpoint is being called first",
    "Invalid or missing credentials"
  ],
  "solutions": [
    "1. CHECK IF API NEEDS AUTHENTICATION:",
    "   - Look at API documentation",
    "   - Check if there's a /login, /auth, or /token endpoint",
    "   - Test with curl: curl -v http://localhost:8080/lab5/users",
    "",
    "2. IF AUTH IS REQUIRED:",
    "   - Open Postman collection in Postman app",
    "   - Add a pre-request script to get auth token",
    "   - Add Authorization header to all requests",
    "   - Format: 'Bearer {token}' or 'Basic {base64_credentials}'",
    "",
    "3. IF NO AUTH IS NEEDED (but getting 401):",
    "   - Check server configuration",
    "   - Verify endpoint URLs are correct",
    "   - Ensure server is running and accessible",
    "",
    "4. QUICK TEST WITH CURL:",
    "   curl -X GET http://localhost:8080/lab5/users",
    "   curl -X POST http://localhost:8080/lab5/login -H 'Content-Type: application/json' -d '{\"username\":\"test\",\"password\":\"test\"}'"
  ],
  "statistics": {
    "totalFailures": 160,
    "errorCode": "401 Unauthorized",
    "affectedEndpoints": [
      "GET All Users",
      "GET User by ID",
      "PUT Update User",
      "POST Create Function",
      "GET All Functions",
      "GET Function by ID",
      "GET Functions by User ID",
      "DELETE User"
    ]
  }
}